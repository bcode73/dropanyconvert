export const engineMeta = {
  id: 'developer_engine',
  version: '0.1.0',
  runtime: 'browser',
};

export async function encodeBase64(files, context) {
  throw new Error('developer_engine.encodeBase64: not yet implemented (Phase 4)');
}

export async function decodeBase64(files, context) {
  throw new Error('developer_engine.decodeBase64: not yet implemented (Phase 4)');
}
